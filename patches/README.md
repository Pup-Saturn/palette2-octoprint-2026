# Patches

The installer currently applies compatibility changes programmatically so that
the repository does not need to redistribute Mosaic's full plugin source.

When the patch set stabilizes, consider replacing the inline transformations
with ordinary unified `.patch` files generated against the exact upstream
Palette 2 3.0.1 and CANVAS 3.0.3 releases.
